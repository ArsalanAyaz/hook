import React from 'react'

export const Print = (props:any) => {
  return (
    <div>
        <h1>hello??</h1>
        {props.title.name}
        <br />
        {props.title.email}
        
        </div>
  )
}
